# Project Instructions

- Keep `layouts.md` synchronized with the active keymap in `config/totem.keymap`. Any change to mappings, layers, layer access, shortcuts, or tap-hold timing must update `layouts.md` in the same change.
