on: [push, pull_request, workflow_dispatch]

jobs:
  build-rb:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: build_rb.yaml
      archive_name: firmware-rb

  build-bw:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: build_bw.yaml
      archive_name: firmware-bw
